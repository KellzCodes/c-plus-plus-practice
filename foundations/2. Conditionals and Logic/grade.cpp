#include <iostream>

int main() {

	// Declare and initialize grade
	int grade = 59; 

	// If grade is greater than 60, print "Pass"
	if (grade > 60) { 
		std::cout << "Pass\n"; 
	}

	// Otherwise, print "Fail"
	else { 
		std::cout << "Fail"; 
	}
}